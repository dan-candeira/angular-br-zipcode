import { ValidateCEPsDirective } from './validate-ceps.directive';

describe('ValidateCEPsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateCEPsDirective();
    expect(directive).toBeTruthy();
  });
});
